package com.dazzle.asklepios.domain.enumeration;

/**
 * Duration Unit for vaccines or medications (e.g. post-opening duration).
 */
public enum DurationUnit {
    SECONDS,
    MINUTES,
    HOURS,
    DAYS,
    WEEKS,
    MONTHS,
    YEARS
}

