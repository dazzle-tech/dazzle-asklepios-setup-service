package com.dazzle.asklepios.domain.enumeration;

/**
 * Fields that can be used to check for patient duplication across facilities.
 */
public enum DuplicationField {
    DOB,
    FIRST_NAME,
    LAST_NAME,
    DOCUMENT_NO,
    MOBILE_NUMBER,
    GENDER
}
