package com.dazzle.asklepios.domain.enumeration;

public enum BillingConfigurationValueType {

    STRING,
    BOOLEAN,
    INTEGER,
    LONG,
    DECIMAL,
    ENUM,
    DATE,
    DATETIME,
    JSON
}