package com.dazzle.asklepios.domain.enumeration;

/**
 * Enum representing how laboratory normal range values are expressed.
 */
public enum NormalRangeType {
    RANGE,
    LESS_THAN,
    MORE_THAN
}
